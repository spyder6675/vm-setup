#!/usr/bin/env bash

# Bump the maximum number of file descriptors you can have open
# Current ulimit 
# ulimit -n 10240


# Check Hostname
# hostname


# Change Date / Time Format 
#date '+%H:%M:%S %p / %A / %B %-d, %Y'


# Creating useful directories
# mkdir -p /root/tools


# Env Setup
# sed 's/colorScheme=Kali-Light/colorScheme=Kali-Dark/' .config/qterminal.org/qterminal.ini

# Git Clone 


# Install Tools
# apt install python3-venv -y
# apt install ntpsec -y
# apt install gedit -y
# apt install urlcrazy -y
# apt install silversearcher-ag -y

cat -n /home/kali/.config/qterminal.org/qterminal.ini | grep -i colorScheme
cat -n /home/kali/.config/qterminal.org/qterminal.ini | grep font
cat -n .config/qterminal.org/qterminal.ini | grep -i TerminalTransparency

######################################################################












# END

